<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      The XML theme exists only to style the window before Compose takes over
      (launch background, status bar). All in-app styling lives in the Compose
      theme in ui/theme/Theme.kt.
    -->
    <style name="Theme.CoffeeGrams" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
