*.iml
.gradle/
build/
captures/
.externalNativeBuild/
.cxx/
local.properties

# Android Studio / IntelliJ
.idea/
*.hprof

# Signing material — NEVER commit these.
# Losing the upload keystore is unrecoverable; back it up outside the repo.
*.jks
*.keystore
keystore.properties

# Signed release build output (e.g. Android Studio's Generate Signed Bundle/APK)
app/release/
*.aab

# macOS
.DS_Store
